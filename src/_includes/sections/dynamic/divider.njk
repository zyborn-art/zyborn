{# Divider Section - Visual separator between sections #}
<div class="section-divider style-{{ section.style | default('line') }} width-{{ section.width | default('medium') }} spacing-{{ section.spacing | default('medium') }}">
  <span class="divider-line"></span>
</div>
