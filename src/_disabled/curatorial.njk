---
layout: base.njk
title: "The Can as Currency — Curatorial Recommendation by Luba Elliott"
permalink: /curatorial/
---

{# CURATORIAL PAGE - Loaded from curatorialData global #}
<article class="article-page">
  {# ARTICLE HERO #}
  <header class="article-hero">
    <div class="container">
      <p class="meta article-type">{{ curatorialData.article_type }}</p>
      <h1 class="article-title">{{ curatorialData.article_title }}</h1>
      
      <div class="article-meta-row">
        <span class="article-author">By {{ curatorialData.author }}</span>
        <span class="article-divider">•</span>
        <span class="article-read-time">{{ curatorialData.read_time }}</span>
        <span class="article-divider">•</span>
        <span class="article-date">{{ curatorialData.publish_date }}</span>
      </div>
    </div>
  </header>

  {# FEATURED IMAGE #}
  {% if curatorialData.featured_image %}
  <figure class="article-featured-image">
    <img src="{{ curatorialData.featured_image.src }}" alt="{{ curatorialData.featured_image.alt }}">
    {% if curatorialData.featured_image.caption %}
      <figcaption class="container">{{ curatorialData.featured_image.caption }}</figcaption>
    {% endif %}
  </figure>
  {% endif %}

  {# ARTICLE BODY #}
  <div class="article-body">
    <div class="container">
      <div class="article-content">
        {% if curatorialData.pull_quote %}
        <blockquote class="article-pull-quote">
          "{{ curatorialData.pull_quote }}"
        </blockquote>
        {% endif %}
        
        {{ curatorialData.content | markdown | safe }}
      </div>

      {# CURATOR BIO #}
      {% if curatorialData.curator %}
      <aside class="curator-bio">
        <h3>About the Author</h3>
        <div class="curator-info">
          <p class="curator-name">{{ curatorialData.curator.name }}</p>
          <p class="curator-title">{{ curatorialData.curator.title }}</p>
          <div class="curator-links">
            {% if curatorialData.curator.website %}
              <a href="{{ curatorialData.curator.website }}" target="_blank" rel="noopener">Website</a>
            {% endif %}
            {% if curatorialData.curator.linkedin %}
              <a href="{{ curatorialData.curator.linkedin }}" target="_blank" rel="noopener">LinkedIn</a>
            {% endif %}
          </div>
        </div>
      </aside>
      {% endif %}
    </div>
  </div>
</article>

{# BACK TO MAIN #}
<section class="article-cta">
  <div class="container">
    <a href="/" class="btn-secondary">← Back to Main Page</a>
  </div>
</section>
